import * as React from "react";
import {
  DropdownMenu as ShadDropdownMenu,
  DropdownMenuContent as ShadDropdownMenuContent,
  DropdownMenuItem as ShadDropdownMenuItem,
  DropdownMenuTrigger as ShadDropdownMenuTrigger,
} from "@radix-ui/react-dropdown-menu";

export const DropdownMenu = ShadDropdownMenu;
export const DropdownMenuContent = ShadDropdownMenuContent;
export const DropdownMenuItem = ShadDropdownMenuItem;
export const DropdownMenuTrigger = ShadDropdownMenuTrigger;
